function detectLang(text){return /[\u0900-\u097F\u0600-\u06FF]/.test(text)?'hi-IN':'en-US';}
function loadVoices(){if(!window.speechSynthesis)return;appState.voices=speechSynthesis.getVoices();}
function speakText(text){if(!window.speechSynthesis||!text)return;const u=new SpeechSynthesisUtterance(text);u.lang=detectLang(text);const v=appState.voices.find(x=>x.lang===u.lang)||appState.voices.find(x=>x.lang.startsWith(u.lang.slice(0,2)));if(v)u.voice=v;speechSynthesis.cancel();speechSynthesis.speak(u);} 
function speakFullReport(){if(!appState.currentUser)return;const txt=`${document.getElementById('ai-feedback').innerText} आपका आज का प्लान: ${appState.currentPlan.map(x=>x.time+' '+x.title).join(', ')}`;speakText(txt);} 
function answerQuestion(q){if(!appState.currentUser)return 'Please login first.';const l=q.toLowerCase();if(l.includes('bmi'))return `Aapka BMI ${appState.currentUser.bmi} hai.`;if(l.includes('plan')||l.includes('task'))return `Aaj ke tasks: ${buildTasks('daily').map(t=>t.title).join(', ')}`;if(l.includes('weight'))return `Current weight ${appState.currentUser.weight} kg hai.`;return `Aapka goal ${appState.currentUser.goal} hai. 6:30 AM aur 5:00 PM class join karein.`;}
function startAssistant(){loadVoices();if(!('webkitSpeechRecognition'in window||'SpeechRecognition'in window)){document.getElementById('assistant-log').innerText='Voice recognition not supported in this browser.';return;}const SR=window.SpeechRecognition||window.webkitSpeechRecognition;appState.recognition=new SR();appState.recognition.continuous=true;appState.recognition.interimResults=false;appState.recognition.lang=(document.documentElement.lang==='hi'?'hi-IN':'en-US');appState.recognition.onresult=(e)=>{const text=e.results[e.results.length-1][0].transcript.trim();document.getElementById('assistant-log').innerText=`You: ${text}`;speakText(answerQuestion(text));};appState.recognition.onend=()=>{if(appState.isListening)appState.recognition.start();};appState.isListening=true;appState.recognition.start();}
function toggleAssistant(){if(!appState.recognition)return;appState.isListening=!appState.isListening;document.getElementById('assistant-toggle').innerText=appState.isListening?'Pause Assistant':'Resume Assistant';if(!appState.isListening){appState.recognition.stop();speechSynthesis.cancel();}else appState.recognition.start();}
if(window.speechSynthesis){speechSynthesis.onvoiceschanged=loadVoices;}
